# set -g @plugin 'tmux-plguins/tpm'
# soet -g @plugin 'christoomey/vim-tmux-navigator'
# set -g @plugin 'catppuccin/tmux'
# set -g @plugin 'tmux-plugins/tmux-yank'
# run '~/.tmux/plugins/tpm/tpm'





set-option -g default-terminal "screen-256color"
set-option -sa terminal-overrides ",xterm*:Tc"

# unbind prefix key from ctrl + b
# unbind C-b

# set alt + p as newo prefix key
# set -g prefix M-p

# Reload the tmux configuration
bind r source-file ~/.tmux.conf; display-message "Configuration reloaded"

# enable mouse soupport
set -g mouse on

# set base index for windows to 1 
set -g base-index 1

# set base index for panes to 1
# set -g pane-base-index 1

# new window
# bind t new-window
# bind n next-window
 
# Split panes using | and -
# bind v split-window -h
# bind o split-window -v

# pane navigation using vim keys
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# Resize panes using Vim keys with prefix
# bind -r H resize-pane -L 5
# bind -r J resize-pane -D 5
# bind -r K resize-pane -U 5
# bind -r L resize-pane -R 5


# status bar to top
set-option -g status-position top

# vim mode when in copy mode
# set-window-option -g mode-keys vi

# necessary for tmux yank to work
# set-option -g set-clipboard on

# Begin selection
# bind-key -T copy-mode-vi v send-keys -X begin-selection
# Copy selection to system clipboard using xclip and cancel selection
# bind-key -T copy-mode-vi y send-keys -X copy-pipe-and-cancel 'xclip -in -selection clipboard'
# bind-key -T copy-mode-vi y send-keys -X copy-pipe-and-cancel 'xsel --clipboard --input'
# Cancel selection without exiting copy mode
# bind-key -T copy-mode-vi x send-keys -X clear-selection
# customize highlight
# set-option -g mode-style bg=colour235,fg=colour136,bold




set-option -g status-bg colour236      # dark gray background
set-option -g status-fg colour252      # light gray foreground
# set-option -g status-left ""
# set-option -g status-right ""
set-option -g status-style "bg=colour236,fg=colour252"

